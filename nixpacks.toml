[phases.setup]
nixPkgs = ["go"]

[phases.install]
cmds = ["go mod download"]

[phases.build]
cmds = ["go build -o app ./cmd/app", "chmod +x app", "go install github.com/gobuffalo/soda@latest"]

[start]
cmd = "export PATH=$PATH:/root/go/bin && soda migrate up && ./app"
